# Starlit Timeline Editor - 変更履歴 📋

## v3.5 (2025/11/14) - レターボックス位置修正（座標変換リセット） 🎬

### 🎯 修正内容

**エフェクト描画前に座標変換を完全リセット！**

#### 🐛 真の原因

座標変換スタックの問題でした：

1. `updatePreview()` が各クリップに対して `renderClip()` を呼ぶ
2. `renderClip()` 内で `ctx.save()` → 座標変換（translate/rotate/scale） → `ctx.restore()`
3. **しかし**: `ctx.restore()` が完全に元に戻らないケースがあった
4. その状態で `applyEffects()` が実行される
5. **結果**: レターボックスが変換された座標系で描画されてしまう

#### ✨ 解決方法

`applyEffects()` の最初に `ctx.setTransform(1, 0, 0, 1, 0, 0)` を追加：

```javascript
applyEffects() {
    const ctx = this.previewCtx;
    const width = this.previewCanvas.width;
    const height = this.previewCanvas.height;
    
    // 座標変換を完全にリセット（重要！）
    ctx.setTransform(1, 0, 0, 1, 0, 0);
    
    // レターボックス描画
    if (this.effects.letterbox.enabled) {
        ctx.fillRect(0, 0, width, this.effects.letterbox.height);
        ctx.fillRect(0, height - this.effects.letterbox.height, width, this.effects.letterbox.height);
    }
    // ...
}
```

#### 📝 `setTransform()` とは

Canvas APIの `setTransform(a, b, c, d, e, f)` メソッド：
- 現在の変換行列を**完全に置き換える**（累積ではない）
- `setTransform(1, 0, 0, 1, 0, 0)` = 単位行列 = **すべての変換をリセット**

**他の方法との違い:**
- `ctx.save()/restore()`: スタック管理だが、戻し忘れがある
- `ctx.resetTransform()`: 同じ効果だが古いブラウザで非対応
- `ctx.setTransform(1,0,0,1,0,0)`: **確実にリセット、互換性◎**

#### 🎨 効果

- ✅ **レターボックス**: 常にキャンバス上端・下端に正確に配置
- ✅ **グラデーション**: キャンバス全体に正確に適用  
- ✅ **座標独立**: 素材の変換に影響されない
- ✅ **確実性**: どんな状態からでもリセット可能

---

## v3.4 (2025/11/14) - フォント変更 🎨

### 🎯 変更内容

**JK丸ゴシックフォントに変更！**

#### ✨ 変更点

1. **フォントファイルの変更**
   - YDWbananaslipplus.otf → **JK-Maru-Gothic-M.otf**

2. **styles.cssの修正**
   - `@font-face` のフォント名を変更
   - `font-family` 指定を変更
   - `font-display: swap` を追加（読み込み最適化）

#### 📝 技術詳細

```css
/* Before */
@font-face {
    font-family: 'YDWbananaslipplus';
    src: url('YDWbananaslipplus.otf') format('opentype');
}

/* After */
@font-face {
    font-family: 'JK Maru Gothic';
    src: url('JK-Maru-Gothic-M.otf') format('opentype');
    font-display: swap;
}
```

#### ⚠️ 重要

**デザイン・カラー・レイアウトは一切変更していません！**
- チョコレート&ビスケット配色: そのまま
- UIレイアウト: そのまま
- 機能: そのまま

**変更したのはフォントファイルの参照のみです。**

---

## v3.3 (2025/11/14) - 素材エクスプローラーの幅調整 📐

### 🎯 改善内容

**タブの改行を防ぐため、素材エクスプローラーの幅を拡大!**

#### ✨ 変更点

1. **左サイドバーの幅を拡大**
   - 250px → **300px** (+50px)

2. **フィルタータブの調整**
   - パディングを増やして余裕を持たせる
   - `white-space: nowrap` で改行を防止
   - 「すべて」が1行で表示されるように

#### 🎨 結果

**Before:**
```
すべ
て  | 画像 | 動画 | 音声 | 連番
```

**After:**
```
すべて | 画像 | 動画 | 音声 | 連番
```

#### 💡 メリット

- タブがすっきり見やすく
- より操作しやすい
- 素材エクスプローラーに余裕

---

## v3.2 (2025/11/14) - フォントサイズさらに拡大 📏

### 🎯 改善内容

**YDWbananaslipplusフォントの細身さを考慮して、さらに大きく読みやすく!**

#### ✨ 変更点

**フォントサイズをさらに拡大:**
- メニュー: 16px → **18px**
- サブメニュー: 15px → **17px**
- ボタン: 15px → **17px**
- 小ボタン: 14px → **16px**
- フィルタータブ: 13px → **15px**
- タイムラインコントロール: 14px → **16px**
- サイドバーヘッダー: 16px → **18px**
- チェックボックス: 13px → **15px**
- トラック数: 15px → **17px**
- ズーム表示: 14px → **16px**
- エクスポート範囲: 14px → **16px**

**パディングも調整:**
- より大きなクリック領域
- 見やすい余白

#### 📝 理由

YDWbananaslipplusフォントは細身で小さく見えるため、「普通より大きすぎる」くらいがちょうど良い!

#### 🎨 結果

- より読みやすく
- 操作しやすい
- 細身のフォントが映える

---

## v3.1 (2025/11/14) - 太字の削除 📝

### 🎯 改善内容

**YDWbananaslipplusフォントの読みやすさを向上させるため、すべての太字を削除!**

#### ✨ 変更点

**太字を削除した箇所:**
1. `.filter-button.active` - アクティブなフィルタータブ
2. `.time-display` - タイムコード表示
3. `.timeline-title` - タイムラインタイトル
4. `.track-count` - トラック数表示
5. `.property-section-header` - プロパティセクションヘッダー
6. `.ae-subproperty label span` - AEプロパティのラベル

#### 📝 理由

YDWbananaslipplusフォントは太字にすると読みづらくなるため、すべて通常の太さに統一しました。

#### 🎨 結果

- より読みやすい
- フォントの美しさが際立つ
- 統一感のあるデザイン

---

## v3.0 (2025/11/14) - フォントの完全適用 🎨

### 🎯 改善内容

**すべてのUI要素にカスタムフォントを適用し、フォントサイズを全体的に拡大!**

#### ✨ 変更点

1. **カスタムフォントの完全適用**
   - メニューバー・サブメニュー
   - ツールバーのボタン
   - 素材フィルタータブ ✅ 修正
   - エクスポート範囲コントロール ✅ 修正
   - タイムラインコントロール ✅ 修正
   - チェックボックスラベル
   - すべてのボタン

2. **フォントサイズの拡大**
   - メニュー: 14px → **16px**
   - サブメニュー: 12px → **15px**
   - ボタン: 13px → **15px**
   - 小ボタン: 12px → **14px**
   - フィルタータブ: 11px → **13px**
   - タイムラインコントロール: 12px → **14px**
   - サイドバーヘッダー: 14px → **16px**
   - チェックボックス: 11px → **13px**

3. **パディングの調整**
   - より見やすく、クリックしやすいサイズに調整

#### 📝 注意

**タイムコード表示(00:00:00.000)は Courier New のまま** - 等幅フォントで見やすさを維持

#### 🎨 視覚的改善

- より統一感のあるデザイン
- 読みやすさの向上
- クリックしやすいUI

---

## v2.9 (2025/11/14) - カスタムフォント＆UI改善 ✨

### 🎯 改善内容

**カスタムフォントを適用し、素材フィルターをシンプルに!**

#### ✨ 変更点

1. **カスタムフォントの適用**
   - YDWbananaslipplus フォントを適用
   - インターフェース全体で使用
   - より統一感のあるデザイン

2. **素材フィルターの改善**
   - タブから絵文字を削除
   - テキストのみのシンプルなデザイン
   - よりクリーンな見た目

#### 🎨 変更内容

**Before (v2.8):**
```
[素材フィルター]
すべて | 🖼️ 画像 | 🎬 動画 | 🎵 音声 | 📹 連番
```

**After (v2.9):**
```
[素材フィルター]
すべて | 画像 | 動画 | 音声 | 連番
```

#### 📦 追加ファイル

- `YDWbananaslipplus.otf` - カスタムフォント

#### 🔧 技術的な変更

**styles.css:**
- `@font-face` でカスタムフォントを読み込み
- `font-family` に適用
- フォールバック設定

**index.html:**
- 素材フィルターボタンから絵文字を削除

---

## v2.8 (2025/11/14) - ボタン配置の最適化 🎯

### 🎯 改善内容

**アンドゥ・リドゥボタンをループ再生の隣に移動し、UIをよりコンパクトに!**

#### ✨ 変更点

1. **ボタン配置の変更**
   - アンドゥ・リドゥボタンをツールバーから移動
   - タイムラインコントロールのループ再生の右隣に配置
   - すべての操作ボタンが一箇所に集約

2. **UI整理**
   - ループ再生の絵文字(🔁)を削除
   - よりシンプルでクリーンなデザイン

#### 🎨 新しいレイアウト

**Before (v2.7):**
```
[ツールバー]
🐻↶ 🐻↷ |

[タイムラインコントロール]
... | 🐻▶ 🐻■ 🔁 ループ再生
```

**After (v2.8):**
```
[ツールバー]
(空)

[タイムラインコントロール]
... | 🐻▶ 🐻■ ループ再生 | 🐻↶ 🐻↷
```

#### 💡 メリット

- ツールバーがすっきり
- 編集関連のボタンが一箇所に集約
- より直感的な配置

---

## v2.7 (2025/11/14) - アンドゥ・リドゥボタン追加 🐻

### 🎯 改善内容

**ツールバーにアンドゥ・リドゥのクマアイコンボタンを追加しました!**

#### ✨ 変更点

1. **ツールバーにアイコンボタン追加**
   - 🐻↶ アンドゥ(元に戻す)ボタン
   - 🐻↷ リドゥ(やり直し)ボタン
   - メニューからもキーボードショートカットからも使える

2. **配置**
   - ツールバーの左側に配置
   - すぐにアクセスできる便利な位置

#### 🎨 レイアウト

```
[ツールバー]
🐻↶ 🐻↷ |

[タイムラインコントロール]
🔍 ズーム | 📊 トラック | ✂️ 分割 | 🐻▶ 🐻■ 🔁 ループ再生
```

#### 💡 使い方

- **アンドゥ**: 🐻↶ ボタンをクリック または `Ctrl+Z`
- **リドゥ**: 🐻↷ ボタンをクリック または `Ctrl+Y`

#### 📦 追加ファイル

- `undo.png` - アンドゥアイコン
- `redo.png` - リドゥアイコン

#### 🔧 技術的な変更

**index.html:**
- ツールバーにアンドゥ・リドゥボタンを追加
- `.icon-button` スタイルを適用
- ツールチップ(title)を追加

---

## v2.6 (2025/11/14) - かわいいクマアイコン 🐻

### 🎯 改善内容

**再生ボタンをかわいいクマのアイコンに変更しました!**

#### ✨ 変更点

1. **カスタムアイコンボタン**
   - 🐻 かわいいクマデザイン
   - **再生** (▶️) → クマの再生アイコン
   - **一時停止** (⏸️) → クマの一時停止アイコン
   - **停止** (⏹️) → クマの停止アイコン

2. **視覚的な改善**
   - ホバー時に背景色が変化
   - クリック時にスケールアニメーション
   - より親しみやすいUI

#### 🎨 デザイン

**Before (v2.5):**
```
▶️ 再生  ⏹️ 停止
(絵文字ボタン)
```

**After (v2.6):**
```
🐻▶  🐻■
(かわいいクマアイコン)
```

#### 🔧 技術的な変更

**ファイル追加:**
- `play.png` - 再生アイコン
- `pause.png` - 一時停止アイコン
- `stop.png` - 停止アイコン

**index.html:**
- ボタンを`<img>`タグを使用したアイコンボタンに変更

**styles.css:**
- `.icon-button` スタイルを追加
- `.button-icon` スタイルを追加
- ホバー/アクティブエフェクトを実装

**app.js:**
- `play()` / `pause()` メソッドで画像を動的に切り替え

---

## v2.5 (2025/11/14) - 再生コントロールの改善 🔁

### 🎯 改善内容

**再生ボタンの配置を最適化し、ループ再生機能を追加しました!**

#### ✨ 変更点

1. **再生/停止ボタンの移動**
   - ツールバーから削除
   - タイムラインコントロールの分割ボタン隣に配置
   - より直感的な位置に

2. **ループ再生機能の追加**
   - 🔁 ループ再生チェックボックスを追加
   - チェックを入れると自動的に最初から再生
   - 長時間の作業時に便利!

#### 🎮 新しいレイアウト

**Before (v2.4):**
```
[ツールバー]
  ▶️ 再生  ⏹️ 停止

[タイムラインコントロール]
  🔍 ズーム | 📊 トラック | ✂️ 分割
```

**After (v2.5):**
```
[ツールバー]
  (空)

[タイムラインコントロール]
  🔍 ズーム | 📊 トラック | ✂️ 分割 | ▶️ 再生 ⏹️ 停止 🔁 ループ再生
```

#### 💡 使い方

1. **通常再生**
   - ▶️ 再生ボタンをクリック
   - 最後まで再生したら自動停止

2. **ループ再生**
   - 🔁 ループ再生にチェック
   - ▶️ 再生ボタンをクリック
   - 最後まで行ったら自動的に最初に戻って再生継続

3. **停止**
   - ⏹️ 停止ボタンで停止
   - または ⏸️ 一時停止ボタン(再生中に表示)

#### 🔧 技術的な変更

**index.html:**
- ツールバーから再生/停止ボタンを削除
- タイムラインコントロールに再生/停止ボタンを追加
- ループ再生チェックボックスを追加

**app.js:**
- `loopPlayback` フラグを追加
- `play()` メソッドにループ処理を実装
- 再生終了時にループ判定を追加

---

## v2.4 (2025/11/14) - 開閉状態の保持機能 🔒

### 🎯 改善内容

**スライダー操作時に勝手に折りたたまれる問題を修正!**

#### 🐛 修正した問題

**Before (v2.3):**
- スライダーを動かすたびにプロパティが折りたたまれる
- 毎回展開し直す必要がある
- 非常にうっとうしい! 😤

**After (v2.4):**
- **開閉状態を記憶**
- スライダーを動かしても状態を維持
- 快適な編集体験! 😊

#### ✨ 変更点

1. **状態保持システムの実装**
   - セクションの開閉状態を保存
   - AEプロパティの展開状態を保存
   - プロパティパネル更新時に自動復元

2. **対象セクション**
   - 📐 トランスフォーム
   - 🎬 トランジション
   - 🔊 音声
   - 各AEプロパティ (位置、スケール、回転、不透明度)

3. **動作例**
```
1. 📐 トランスフォームを展開
2. ▶ 位置を展開
3. Xスライダーを調整
   ↓
✅ 展開状態を維持!
(以前は勝手に折りたたまれていた)
```

#### 🔧 技術的な変更

**app.js:**
- `propertySectionStates` オブジェクトを追加
- `aePropertyStates` オブジェクトを追加
- `togglePropertySection()` に状態保存処理を追加
- `toggleAEProperty()` に状態保存処理を追加
- `updatePropertiesPanel()` に状態復元処理を追加

---

## v2.3 (2025/11/14) - プロパティ順序の最適化 📐

### 🎯 改善内容

**プロパティの表示順序を最適化し、より使いやすく整理しました!**

#### ✨ 変更点

**新しいプロパティ順序:**
1. ✅ **クリップ名**
2. ✅ **開始時間**
3. ✅ **継続時間**
4. ✅ **フレームレート** (連番の場合のみ、セクション化せず直接表示)
5. ✅ **📐 トランスフォーム** (AE風階層UI)
6. ✅ **🎬 トランジション**
7. ✅ **🔊 音声** (音声/動画の場合のみ)

#### 💡 改善ポイント

1. **フレームレートを継続時間の直後に配置**
   - 連番アニメーションで重要な設定
   - セクション化せず、シンプルなスライダーとして表示
   - より直感的にアクセス可能

2. **トランスフォームを上部に移動**
   - 最も頻繁に使用するプロパティ
   - AE風の階層UIで見やすく

3. **論理的な順序**
   - タイミング設定 (開始/継続/フレームレート)
   - 視覚的変形 (トランスフォーム)
   - エフェクト (トランジション)
   - オーディオ (音声)

---

## v2.2 (2025/11/14) - ループ処理の改善とUI整理 🔁

### 🎯 修正内容

**ループ処理を直感的に改善し、不要なUIを削除しました!**

#### 🐛 修正した問題

**Before (v2.1):**
- 継続時間がループ回数と連動していた
- 3秒の動画で「ループ回数3」に設定すると、継続時間を9秒に引き延ばす必要があった
- 直感的でなく、混乱しやすい仕様
- 不要な「ループ設定」セクションがあった

**After (v2.2):**
- **継続時間を設定するだけで自動的にループ**
- 3秒の動画を9秒に引き延ばせば、自動的に3回ループ再生される
- シンプルで直感的!
- **ループ設定セクションを削除** (不要なUIを整理)

#### ✨ 変更点

1. **自動ループ再生**
   - 継続時間内で素材が自動的に繰り返し再生
   - ループ回数の設定が不要に
   - 継続時間を調整するだけでループ回数が決まる

2. **UI整理**
   - 「🔁 ループ」セクションを削除
   - 連番アニメーションの場合のみ「▶️ 再生設定」を表示
   - フレームレート設定のみが残る
   - よりシンプルで分かりやすいUI

3. **動作例**
```
元の動画: 3秒
継続時間を9秒に設定
→ 自動的に3回ループ再生! (3秒 × 3回 = 9秒)

元の動画: 2秒
継続時間を7秒に設定
→ 3.5回ループ再生! (2秒 × 3.5回 = 7秒)

連番画像: 30フレーム、30fps
継続時間を3秒に設定
→ 3回ループ再生!
```

#### 🔧 技術的な変更

**app.js:**
- `renderClip()` メソッドのループ処理を修正
- `loopCount` プロパティの使用を廃止
- 素材の元の長さで自動的にモジュロ演算
- ループセクションのUI削除
- 連番の場合のみ「再生設定」セクションを表示

---

## v2.1 (2025/11/14) - After Effects風プロパティUI 🎬

### 🎯 実装内容

**After Effectsをイメージした階層的なプロパティUI**を実装!プロフェッショナルな動画編集体験を提供します。

### ✨ 主な変更点

#### 1. **AE風トランスフォームUI**
トランスフォームプロパティが階層的に整理され、より直感的に:

- **📍 位置** - X/Yを展開して個別調整
- **🔍 スケール** - 一括スケール調整
- **🔄 回転** - 回転角度の調整
- **👁️ 不透明度** - 透明度の調整

各プロパティは **▶ をクリック** して展開/折りたたみ可能!

#### 2. **キーフレームインジケーター 💎**
- 💎 アイコンが各プロパティの右側に表示
- キーフレームが設定されているとオレンジ色に光る
- ワンクリックでキーフレーム追加/削除
- 視覚的に分かりやすいフィードバック

#### 3. **値の表示改善**
- プロパティ名の横に現在値を常時表示
- 展開せずに値を確認可能
- スライダー調整時にリアルタイム更新

#### 4. **コンパクトなデザイン**
- 必要なプロパティだけ展開して作業
- スクロールを最小限に抑えた効率的なレイアウト
- AEユーザーにも馴染みやすいUI

### 🎨 UI比較

**Before (v2.0):**
```
📐 トランスフォーム ▼
  X位置: 0px [================] 💎キーフレーム
  Y位置: 0px [================] 💎キーフレーム
  回転: 0° [=================] 💎キーフレーム
  不透明度: 100% [============] 💎キーフレーム
  スケール: 100% [============] 💎キーフレーム
```

**After (v2.1) - AE風:**
```
📐 トランスフォーム ▼
  ▶ 📍 位置                    0, 0 💎
  ▶ 🔍 スケール               100% 💎
  ▶ 🔄 回転                     0° 💎
  ▶ 👁️ 不透明度              100% 💎
  
展開すると:
  ▼ 📍 位置                    0, 0 💎
     X: 0px     [================]
     Y: 0px     [================]
```

### 🔧 技術的な変更

**app.js:**
- `toggleAEProperty(propertyName)` メソッドを新規追加
- トランスフォームセクションを階層構造に再設計
- キーフレームインジケーターの状態管理を改善

**styles.css:**
新しく追加したスタイル:
- `.ae-property-group` - AE風プロパティグループ
- `.ae-property-header` - 展開可能なプロパティヘッダー
- `.ae-property-icon` - 展開/折りたたみアイコン (▶/▼)
- `.ae-keyframe-indicator` - キーフレーム状態の表示
- `.ae-property-value` - プロパティ値の表示
- `.ae-property-content` - 展開可能なコンテンツエリア
- `.ae-subproperty` - サブプロパティ (X/Yなど)

### 🚀 使い方

1. クリップを選択
2. 📐 **トランスフォーム**セクションを展開
3. **▶ アイコン**をクリックして各プロパティを展開
4. スライダーで値を調整
5. **💎 ボタン**でキーフレーム追加!

### 🎬 After Effects との比較

| 機能 | After Effects | Starlit Timeline Editor |
|------|---------------|------------------------|
| 階層的プロパティ | ✅ | ✅ |
| キーフレーム表示 | ✅ | ✅ |
| 値のプレビュー | ✅ | ✅ |
| ワンクリック展開 | ✅ | ✅ |
| 色分け表示 | ✅ | 🔶 (部分的) |
| グラフエディター | ✅ | ❌ (予定) |

---

## v2.0 (2025/11/14) - プロパティセクション開閉機能

### 🎯 実装内容

プロパティパネルの各セクションに開閉機能を追加!

### ✨ 主な変更点

- **トランジション** (🎬) セクション
- **音声** (🔊) セクション
- **ループ** (🔁) セクション  
- **トランスフォーム** (📐) セクション

各セクションヘッダーをクリックすると開閉できます。

---

## v1.0 (初回リリース)

- キーフレームアニメーション
- 動画書き出し
- エフェクト機能
- トランジション機能
- 音声レイヤーのサポート

---

**Made with ❤️ by Ambrose Starlit VTuber**
**Target: After Effects Alternative for Browser! 🎯**
